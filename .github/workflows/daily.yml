name: daily-video-deprecated
# This workflow is replaced by render-nightly + upload-scheduled
# Kept for reference only
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use render-nightly and upload-scheduled workflows instead"
